---
- name: touch /var/log/kube-scheduler.log
  command: touch /var/log/kube-scheduler.log

- name: create kube-scheduler
  template: 
    src: kube-scheduler.yaml
    dest: /etc/kubernetes/manifests/kube-scheduler.yaml
    owner: root
    group: root
    mode: 0644
